{
  "name": "tacheflow",
  "version": "1.0.0",
  "description": "Smart Task Management System for Students",
  "main": "server.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-deps": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "task-management",
    "student",
    "mern",
    "react",
    "nodejs"
  ],
  "author": "TâcheFlow Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}